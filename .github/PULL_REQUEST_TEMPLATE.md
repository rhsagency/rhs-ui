## What

<!-- One item per PR. Which item, what changed, and why. Link the issue for a new item. -->

## Checks

- [ ] `pnpm check` passes (typecheck + registry gate)
- [ ] `pnpm registry:build` was run and `public/r` is committed
- [ ] Keyboard, focus and roles verified; text next to colour for every state
- [ ] Light and dark verified
- [ ] `meta.version` bumped when files changed, and `CHANGELOG.md` has a line
- [ ] No forbidden dependency (lucide-react, cn, shadcn, @base-ui, scoped @radix-ui)

## Notes

<!-- Anything a reviewer should look at first. -->
